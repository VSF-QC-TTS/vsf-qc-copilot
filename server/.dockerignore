# Build artifacts
server/target/
server/.idea/

# Docs (not needed at runtime)
docs/

# Git
.git/
.gitignore

# Agent/IDE config
.agents/
.codex/

# Local env files (secrets injected via Docker env vars)
server/.env

# Node modules (rebuilt in Docker)
tooling/promptfoo-runner/node_modules/
